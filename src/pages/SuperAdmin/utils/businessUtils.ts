// src/pages/SuperAdmin/utils/Businesses/businessUtils.ts
// Add your reusable business-related utility functions here.

export const formatBusinessName = (name: string) => name.trim().toUpperCase();
